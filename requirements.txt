fastapi
uvicorn
pymupdf
python-docx
openpyxl  # gera XLSX (Fase 4, Camada 1 -- planilha de preço)
jinja2
python-multipart
python-dotenv
google-genai
pyyaml  # lê app/extracao/palavras_chave_item.yaml (extrator de exigências por item, Passo 8)

# Dependências de desenvolvimento/teste NÃO vão aqui — ver
# requirements-dev.txt. Separado de propósito (Fase 3, deploy no Render):
# este arquivo é o que o build de produção instala, e playwright em
# particular baixaria um navegador inteiro à toa num servidor que nunca
# roda teste nenhum.
